// lib/data/models/orden_compra/orden_compra_model.dart

// Simplemente un marcador de posición para evitar errores de compilación
// ya que el Notifier lo importa. Si no usas Tipos fuertes aquí, puedes
// borrar la importación en el Notifier y este archivo no es necesario.
// Si lo quieres Tipado, necesitarías definir:

/*
class OrdenCompraResumen {
  final int id;
  final String proveedor;
  final String tipoCompra;
  // ... otros campos
  
  OrdenCompraResumen({required this.id, required this.proveedor, required this.tipoCompra});

  factory OrdenCompraResumen.fromJson(Map<String, dynamic> json) {
    return OrdenCompraResumen(
      id: json['id'] as int,
      proveedor: json['proveedor'] as String,
      tipoCompra: json['tipo_compra'] as String,
    );
  }
}
*/